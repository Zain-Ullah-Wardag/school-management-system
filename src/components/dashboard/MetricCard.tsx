import type { ReactNode } from 'react';
import { TrendingDown, TrendingUp } from 'lucide-react';
import { Card } from '../common/Card';
export function MetricCard({ label, value, icon, tone = 'brand', helper, trend }: { label: string; value: ReactNode; icon: ReactNode; tone?: 'brand' | 'blue' | 'amber' | 'rose' | 'violet'; helper?: string; trend?: 'up' | 'down' }) { const tones = { brand: 'bg-brand-50 text-brand-600', blue: 'bg-sky-50 text-sky-600', amber: 'bg-amber-50 text-amber-600', rose: 'bg-rose-50 text-rose-600', violet: 'bg-violet-50 text-violet-600' }; return <Card className="p-4"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">{label}</p><p className="mt-2 text-2xl font-extrabold tracking-tight text-slate-900">{value}</p>{helper && <span className="mt-2 inline-flex items-center gap-1 text-xs font-medium text-slate-500">{trend === 'up' ? <TrendingUp className="h-3.5 w-3.5 text-brand-600" /> : trend === 'down' ? <TrendingDown className="h-3.5 w-3.5 text-rose-600" /> : null}{helper}</span>}</div><span className={`rounded-xl p-2.5 ${tones[tone]}`}>{icon}</span></div></Card>; }
