export type TabItem<T extends string> = { id: T; label: string; count?: number };
export function Tabs<T extends string>({ tabs, value, onChange }: { tabs: TabItem<T>[]; value: T; onChange: (value: T) => void }) { return <div className="no-print mb-5 flex max-w-full gap-1 overflow-x-auto rounded-2xl border border-slate-100 bg-white p-1.5 shadow-panel">{tabs.map((tab) => <button key={tab.id} onClick={() => onChange(tab.id)} className={`whitespace-nowrap rounded-xl px-3 py-2 text-sm font-semibold transition ${value === tab.id ? 'bg-brand-600 text-white shadow-sm' : 'text-slate-500 hover:bg-slate-50 hover:text-slate-800'}`}>{tab.label}{tab.count !== undefined && <span className={`ml-1.5 text-xs ${value === tab.id ? 'text-brand-100' : 'text-slate-400'}`}>{tab.count}</span>}</button>)}</div>; }
