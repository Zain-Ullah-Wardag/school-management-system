import type { Request } from 'express';
import { AssessmentService } from '../services/assessment.service';
import { asyncHandler,message,parseId,success } from '../utils/http';
import type { AuthenticatedRequest } from '../types';
import { logActivity } from '../utils/activity';
const service=new AssessmentService();const user=(r:Request)=>(r as AuthenticatedRequest).user!;
export const listTests=asyncHandler((req,res)=>success(res,service.tests(req.query)));export const getTest=asyncHandler((req,res)=>success(res,service.test(parseId(req.params.id,'test id'))));
export const saveTest=asyncHandler((req,res)=>{const item=service.saveTest(req.body,user(req).id,req.params.id?parseId(req.params.id):undefined);success(res,item,req.params.id?200:201);});export const testMarks=asyncHandler((req,res)=>success(res,service.testMarks(parseId(req.params.id,'test id'))));export const saveTestMarks=asyncHandler((req,res)=>{const id=parseId(req.params.id,'test id');success(res,service.saveTestMarks(id,req.body.marks||[],user(req).id));});export const publishTest=asyncHandler((req,res)=>{service.publishTest(parseId(req.params.id,'test id'));message(res,'Class test published');});
export const listExams=asyncHandler((req,res)=>success(res,service.exams(req.query)));export const getExam=asyncHandler((req,res)=>success(res,service.exam(parseId(req.params.id,'exam id'))));export const saveExam=asyncHandler((req,res)=>success(res,service.saveExam(req.body,user(req).id,req.params.id?parseId(req.params.id):undefined),req.params.id?200:201));
export const saveExamSubject=asyncHandler((req,res)=>success(res,service.saveExamSubject(parseId(req.params.id,'exam id'),req.body,req.params.subjectId?parseId(req.params.subjectId,'exam subject id'):undefined),req.params.subjectId?200:201));export const examMarks=asyncHandler((req,res)=>success(res,service.marks(parseId(req.params.subjectId,'exam subject id'))));export const saveExamMarks=asyncHandler((req,res)=>success(res,service.saveMarks(parseId(req.params.subjectId,'exam subject id'),req.body.marks||[],user(req).id)));
export const transition=asyncHandler((req,res)=>{const action=req.params.action as 'submit'|'approve'|'publish';if(action!=='submit'&&!user(req).permissions.includes('assessments.approve')) throw new (require('../utils/errors').ApiError)(403,'Principal approval permission is required');const item=service.transition(parseId(req.params.id,'exam id'),action,user(req).id);logActivity(user(req).id,action,'exam',parseId(req.params.id),'Exam status changed');success(res,item);});export const results=asyncHandler((req,res)=>success(res,service.results(parseId(req.params.id,'exam id'),Number(req.query.class_id),req.query.section_id?Number(req.query.section_id):null)));
